apiVersion: v1
kind: Service
metadata:
  name: {{ include "audionex.fullname" . }}
  labels:
    {{- include "audionex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "audionex.selectorLabels" . | nindent 4 }}
